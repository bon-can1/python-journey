# input we are give the public to give there input or data by them
a = input("Enter your data: ") # by default we get str input 
b = int(input("enter input: ")) # we make the user input only to integer
c = float(input("enter float input: ")) # we make the user input to float or decimel input
print(f"string {a} integer{b} float {c}")