import math
pi = math.pi

num = -23

print(pi) # print the value of pi
print(round(math.pi, 2))  # round the value in 2 decimel we can change it by changing the "2" to how much decimal we want
print(math.ceil(pi))  # round the value by highest possible
print(math.floor(pi)) # round the value by nearest lowest possible
print(math.factorial(5))  # it will give the factorial of the value 
print(math.pow(5,2)) # give the power or exponential of the 1st number with last number
print(abs(num))  # give the abselute value
print(math.sqrt(4)) # give the square of the value